# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.env
.env.*
.venv/
venv/
.env
# Model/artifacts
# NOTE: We intentionally KEEP model artifacts in git (via Git LFS) so the repo
# is self-contained. Do not add `src/models/` here.
# VS Code
.vscode/